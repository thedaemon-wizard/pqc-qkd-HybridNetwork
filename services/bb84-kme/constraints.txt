# Pip CONSTRAINTS for the editable backend installs (SimQN / SeQUeNCe /
# Strawberry Fields / TNO) in the Dockerfile. Constraints pin versions WITHOUT
# installing anything and WITHOUT extras (pip rejects extras in a -c file, so
# uvicorn appears here without [standard]).
#
# Purpose: stop a transitive dependency of the heavy backends from moving the web
# stack off the fastapi/starlette pair pinned in requirements.txt. A mismatched
# pair once made the KME crash on import with
# `Router.__init__() got an unexpected keyword argument 'on_startup'`. Keep these
# two lines identical to requirements.txt (the reason for 1.7.0 is recorded there).
fastapi==0.141.1
starlette==1.7.0
# Same two as requirements.txt, where the reason is recorded: SeQUeNCe's
# editable install otherwise moves them past the pins.
numpy==2.5.3
qutip==5.3.1
uvicorn==0.32.1
pydantic==2.10.4
# Same as requirements.txt, where the reason is recorded: SeQUeNCe's install
# moved pyyaml past the old pin, and pandas had none.
pyyaml==6.0.3
pandas==3.0.6
# Strawberry Fields (cvqkd backend) imports the legacy `pkg_resources`, which
# setuptools >=81 removed. SeQUeNCe's install otherwise upgrades setuptools to
# 82 and breaks the cvqkd backend ("No module named 'pkg_resources'"). Hold it.
# Permanent while Strawberry Fields is a dependency: upstream is archived.
setuptools<81
